def divide(a: int, b: int) -> float:
    """ Return the quotient of <a> divided by <b>.

    Precondition: b > 0
    >>> divide(5, 2)
    2.5
    >>> divide(10, 2)
    5.0
    >>> divide(0, 1)
    0.0
    >>> divide(100, 10)
    10.0
    """
    return a / b
